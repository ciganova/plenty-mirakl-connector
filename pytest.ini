[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: tests that hit a real Postgres / external API (skipped by default)
filterwarnings =
    ignore::DeprecationWarning

